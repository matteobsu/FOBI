function [imageflt] = DataFiltering(image,kernelsize,kerneltype)
%DATAFILTERING Summary of this function goes here
%   Detailed explanation goes here
if(dim(kernel)==2)
	if(kerneltpye='movingaverage')
		K = ones(kernelsize(1),kernelsize(2),1);
		K = K./sum(K(:));
		for ii=1:	
end
if(dim(kernel)==2)
end
end

